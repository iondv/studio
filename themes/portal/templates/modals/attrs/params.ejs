<%
  params.name = typeof name === 'undefined' ? '' : name;
  params.required = typeof required === 'undefined' || !required ? '' : ' required';
  params.label = typeof label === 'undefined' ? '' : label;
  params.placeholder = typeof placeholder === 'undefined' ? '' : placeholder;
  params.hint = typeof hint === 'undefined' ? '' : hint;
  params.defaultValue = typeof defaultValue === 'undefined' ? '' : defaultValue;
  params.value = typeof value === 'undefined' ? params.defaultValue : value;
  params.cssClass = typeof cssClass === 'undefined' ? '' : cssClass;
  params.mask = typeof mask === 'undefined' ? '' : typeof mask === 'string' ? mask : JSON.stringify(mask);
  params.readOnly = typeof readOnly === 'undefined' ? '' : readOnly ? 'readonly' : '';
  params.action = typeof action === 'undefined' ? '' : action;
%>