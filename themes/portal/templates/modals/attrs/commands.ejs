<!-- commands -->
<%
  const params = {};
  const items = [{
    id: 'CREATE',
    caption: 'Create',
    visibilityCondition: null,
    enableCondition: null,
    needSelectedItem: false,
    signBefore: false,
    signAfter: false,
    isBulk: false
  },{
    id: 'EDIT',
    caption: 'Edit',
    visibilityCondition: null,
    enableCondition: null,
    needSelectedItem: true,
    signBefore: false,
    signAfter: false,
    isBulk: false
  },{
    id: 'DELETE',
    caption: 'Delete',
    visibilityCondition: null,
    enableCondition: null,
    needSelectedItem: true,
    signBefore: false,
    signAfter: false,
    isBulk: false
  },{
    id: 'ADD',
    caption: 'Add',
    visibilityCondition: null,
    enableCondition: null,
    needSelectedItem: false,
    signBefore: false,
    signAfter: false,
    isBulk: false
  },{
    id: 'REMOVE',
    caption: 'Remove',
    visibilityCondition: null,
    enableCondition: null,
    needSelectedItem: true,
    signBefore: false,
    signAfter: false,
    isBulk: false
  }];
  const defaults = ['CREATE', 'ADD', 'EDIT'];
%>
<%- partial('./params', {params}) %>

<div class="attr form-group<%- params.required %>"
     data-type="commands"
     data-name="<%- params.name %>"
     data-extension="<%= params.extension %>"
     data-action="<%- params.action %>">
  <label class="control-label col-sm-3 l10n"><%- params.label %></label>
  <div class="col-sm-9">
    <input type="hidden" class="form-control value" value="">
    <div class="commands" data-params="<%= JSON.stringify({items, defaults}) %>">
      <% for (let item of items) { %>
      <button class="btn btn-default l10n" type="button"
              data-l10n="commands"
              data-command="<%- item.id %>"><%- item.caption%></button>
      <% } %>
    </div>
    <div class="hint-block l10n"><%- params.hint %></div>
    <div class="error-block"></div>
  </div>
</div>
